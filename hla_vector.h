
#ifndef HLA_VECTOR_H
#define HLA_VECTOR_H

#include <vector>

template <typename T>
class hla_vector {
    
    std::vector<T> vec;
    
public:
    
    /* ctor */
    hla_vector(std::vector<T> vec_);
    
private:
    
private:
    
};

#endif
